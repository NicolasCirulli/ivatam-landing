---
import { SITE } from "../config/site";

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  title = "IVATAM | Repuestos y Taller Automotor en Ezpeleta",
  description = "Repuestos y servicio técnico de frenos, suspensión, tren delantero y embrague en Ezpeleta. Primeras marcas, asesoramiento personalizado e instalación.",
  image = "/og.png",
} = Astro.props;

// Construimos la URL canónica (el dominio se toma de `site` en astro.config.mjs)
const canonicalURL = new URL(Astro.url.pathname, Astro.site || SITE.domain);

// Schema Org para Negocio Local
const localBusinessSchema = {
  "@context": "https://schema.org",
  "@type": ["AutoPartsStore", "AutoRepair"],
  "name": SITE.name,
  "image": new URL(image, canonicalURL).toString(),
  "@id": canonicalURL.toString(),
  "url": canonicalURL.toString(),
  "telephone": SITE.schemaPhone,
  "address": {
    "@type": "PostalAddress",
    "streetAddress": SITE.address,
    "addressLocality": SITE.city,
    "addressRegion": SITE.province,
    "postalCode": SITE.postalCode,
    "addressCountry": "AR"
  },
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": [
      "Monday",
      "Tuesday",
      "Wednesday",
      "Thursday",
      "Friday"
    ],
    "opens": "08:00",
    "closes": "18:00"
  }
};
---

<!-- Global Metadata -->
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="icon" href="/favicon.ico" sizes="any" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
<meta name="generator" content={Astro.generator} />
<meta name="theme-color" content="#020617" />

<!-- Canonical URL -->
<link rel="canonical" href={canonicalURL} />

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
<meta name="keywords" content="repuestos, tren delantero, frenos, embrague, Ezpeleta, Quilmes, Zona Sur, amortiguadores, pastillas de freno, taller mecánico, autopartes" />

<!-- Open Graph / Facebook / WhatsApp -->
<meta property="og:type" content="website" />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, canonicalURL)} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:image:alt" content="IVATAM, repuestos y taller automotor en Ezpeleta" />
<meta property="og:locale" content="es_AR" />
<meta property="og:site_name" content={SITE.name} />

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:url" content={canonicalURL} />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={new URL(image, canonicalURL)} />
<meta name="twitter:image:alt" content="IVATAM, repuestos y taller automotor en Ezpeleta" />

<!-- Schema Markup: LocalBusiness -->
<script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />
